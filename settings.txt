   "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "gcc",
            "args": [
                "main.c", "-o", "main.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]

flags: 	-Wall - включает все разумные предупрежденияя
	-g    - заставляет компилятор сгенерировать отладочную информацию.
	-o    - позволяет задать имя файла, в который будет записан результат компиляции